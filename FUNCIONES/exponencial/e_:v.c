#include <stdio.h>
int main(void)
{
	int x, resultado, factorial, exponente, suma = 0, div;
	printf("Ingrese el valor de x : ");
		scanf("%d", &x);
	for (int i = 2; i < count; ++i)
			{
				
			}		
	return 0;
}

double exponente(double base, double exponente )
{
	int var;
		var = base;	
		for (int i = 2; i <= exponente; ++i)
		{	
			x = base * var;
			var = x;		
		}
		return exponente_r;
}

double factorial(double )
{
	for (int i = 1 ; i <= numero; ++i)
		{
			count = var * i;  
			var = count;       	
		}
	return factorial;
}